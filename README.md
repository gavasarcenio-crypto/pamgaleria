comandos para testar o codigo no terminal do projeto:

git clone -b master https://github.com/gavasarcenio-crypto/pamgaleria.git
cd /pamgaleria
npm i 
npx install -g expo-cli
npx expo install expo-image-picker
npx expo install expo-contacts
npx expo start --tunnel
